package principal;

import javax.swing.JOptionPane;

/**
 *
 * @author Aluno
 */
public class Principal {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        Calculos obj = new Calculos(2,3,0);
        obj.Somar();
        obj.setValor1(7);
        obj.Somar();
        double x;
        x= Double.parseDouble(JOptionPane.showInputDialog(cmpnt, x, x))
    }
    
}
